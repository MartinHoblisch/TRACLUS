% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_result.R
\name{plot_traclus_result}
\alias{plot_traclus_result}
\title{Plot TRACLUS clustering results}
\usage{
plot_traclus_result(
  clustered_segs,
  representatives = NULL,
  mode = c("clusters", "representatives"),
  title = "TRACLUS clustering result",
  noise_col = "grey80",
  cluster_col = "grey60",
  noise_lwd = 0.5,
  seg_lwd = 1.5,
  rep_lwd = 4,
  pal = NULL,
  show_legend = TRUE
)
}
\arguments{
\item{clustered_segs}{Data frame with columns \code{sx}, \code{sy}, \code{ex}, \code{ey},
and \code{cluster_id} (as returned by \code{cluster_segments} or \code{traclus()$segments}).}

\item{representatives}{Named list of representative trajectory matrices
(as returned by \code{compute_all_representatives} or \code{traclus()$representatives}).
If \code{NULL}, only segments are plotted.}

\item{mode}{Character string controlling how clusters are displayed:
\describe{
\item{\code{"clusters"}}{Each cluster's segments and representative trajectory share the
same colour. The representative is drawn with a black outline to stand out from
the underlying segments. This is the default.}
\item{\code{"representatives"}}{Clustered segments are drawn in a uniform grey (solid,
not dashed). Only the representative trajectories are coloured, making them the
visual focus.}
}}

\item{title}{Plot title. Default \code{"TRACLUS clustering result"}.}

\item{noise_col}{Colour for noise segments. Default \code{"grey80"}.}

\item{cluster_col}{Colour for clustered segments in \code{"representatives"} mode.
Default \code{"grey60"}.}

\item{noise_lwd}{Line width for noise segments. Default 0.5.}

\item{seg_lwd}{Line width for clustered segments. Default 1.5.}

\item{rep_lwd}{Line width for representative trajectories. Default 4.}

\item{pal}{Character vector of colours used for clusters / representatives. Cycles if there
are more clusters than colours. Defaults to the Wong (2011) colorblind-safe palette.}

\item{show_legend}{Logical. If \code{TRUE} (default), a legend is drawn.}
}
\value{
Invisibly \code{NULL}, called for its side effect.
}
\description{
Visualises the output of \code{cluster_segments} or \code{traclus}: noise segments in grey,
clustered segments and their representative trajectories in colour. Two display modes are
available (see \code{mode}).
}
\examples{
result <- traclus(make_toy_trajectories(), eps = 10, min_lns = 3)
plot_traclus_result(result$segments, result$representatives)
plot_traclus_result(result$segments, result$representatives, mode = "representatives")
}
