% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traclus_partition.R
\name{partition_trajectory}
\alias{partition_trajectory}
\title{Partition a single trajectory into line segments using the MDL principle}
\usage{
partition_trajectory(traj)
}
\arguments{
\item{traj}{Numeric matrix with columns x, y. At least 2 rows.}
}
\value{
A list of line segments. Each element is a named list with si and ei (numeric vectors of length 2).
}
\description{
Greedy algorithm. Scans the trajectory point by point. When partitioning becomes cheaper than keeping
the raw sub-segments, the previous point is saved as a characteristic point and the scan restarts from there.
}
\examples{
traj <- matrix(c(0,0, 5,1, 10,0, 15,5, 20,10), ncol=2, byrow=TRUE,
               dimnames=list(NULL, c("x","y")))
segs <- partition_trajectory(traj)
length(segs)
}
